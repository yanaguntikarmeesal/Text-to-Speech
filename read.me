# 🎙️ AI Vocalist — Text-to-Speech

A simple and user-friendly **Text-to-Speech (TTS) web application** built with **Python, Streamlit, and Google Text-to-Speech (gTTS)**.

The application converts user-entered text into an **MP3 audio file** in multiple languages such as English, Hindi, Kannada, Spanish, French, German, Japanese, and Chinese.

## 🚀 Features

* 🎙️ Convert text into speech
* 🌐 Supports multiple languages
* 🔊 Play generated audio directly in the browser
* 📥 Download speech as an MP3 file
* 📝 Custom output filename
* 🎨 Clean and responsive Streamlit interface
* ⚡ Simple and beginner-friendly implementation
* ☁️ Can be deployed using Streamlit-compatible hosting

## 🌍 Supported Languages

| Language      | Code    |
| ------------- | ------- |
| 🇮🇳 Hindi    | `hi`    |
| 🇬🇧 English  | `en`    |
| 🇮🇳 Kannada  | `kn`    |
| 🇪🇸 Spanish  | `es`    |
| 🇫🇷 French   | `fr`    |
| 🇩🇪 German   | `de`    |
| 🇯🇵 Japanese | `ja`    |
| 🇨🇳 Chinese  | `zh-cn` |

## 🛠️ Technologies Used

* **Python**
* **Streamlit**
* **gTTS (Google Text-to-Speech)**
* **HTML/CSS** for interface styling

## 📁 Project Structure

```text
AI-Vocalist/
│
├── app.py
├── requirements.txt
└── README.md
```

## ⚙️ Installation

### 1. Clone the repository

```bash
git clone https://github.com/your-username/AI-Vocalist.git
```

### 2. Open the project

```bash
cd AI-Vocalist
```

### 3. Install required libraries

Open the VS Code terminal and run:

```bash
python -m pip install streamlit gTTS
```

Or create a `requirements.txt` file:

```text
streamlit
gTTS
```

Then install:

```bash
python -m pip install -r requirements.txt
```

## ▶️ Run the Application

Run the following command:

```bash
python -m streamlit run app.py
```

The application will open in your web browser.

## 🧑‍💻 How to Use

1. Select a language.
2. Enter an optional filename.
3. Type or paste your text.
4. Click **🚀 Generate Audio**.
5. Listen to the generated audio.
6. Click **📥 Download MP3** to save the audio.

## 🔄 How the Application Works

```text
User enters text
       ↓
Select language
       ↓
Streamlit receives input
       ↓
gTTS converts text → speech
       ↓
MP3 file is generated
       ↓
Audio player displays the result
       ↓
User downloads MP3
```

## 📌 Important Note

**gTTS requires an active internet connection** because the application communicates with Google's Text-to-Speech service to generate the audio.

## 🎓 Learning Objectives

This project is useful for learning:

* Python programming
* Streamlit application development
* User input handling
* Dictionaries in Python
* Functions and variables
* Exception handling
* File handling
* Text-to-Speech
* Audio file generation
* Basic HTML/CSS styling in Streamlit

## 🔮 Future Improvements

Possible enhancements include:

* 🎚️ Speech speed control
* 🎵 Voice selection
* 🔊 Volume controls
* 📂 Multiple audio formats
* 🗣️ More languages
* 🎤 Speech-to-Text integration
* 🤖 AI-powered voice generation
* 📱 Improved mobile interface

## 👨‍💻 Developer

**Yanaguntikar Meesal**

Built with ❤️ using **Python + Streamlit + gTTS**.

---

### 📄 `requirements.txt`

For your GitHub repository, create a file named **`requirements.txt`** containing:

```text
streamlit
gTTS
```

Then your GitHub project can have:

```text
AI-Vocalist/
│
├── app.py
├── requirements.txt
└── README.md
```

This is the structure Streamlit deployment platforms can use to install the required Python libraries automatically.
